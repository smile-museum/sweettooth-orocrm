{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set pointsHistoryWidget %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('sweettooth_loyalty_widget_points_history', {contactId: entity.id}),
        'title': 'Points History',
        'alias': 'contact_points_history'
    }) }}
{% endset %}
{{ UI.scrollSubblock(null, [pointsHistoryWidget]) }}

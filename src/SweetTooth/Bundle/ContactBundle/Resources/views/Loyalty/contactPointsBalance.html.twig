{% if resource_granted('orocrm_call_view') %}
    {% import 'OroUIBundle::macros.html.twig' as UI %}
    
    {% set pointsBalanceWidget %}
        {{ oro_widget_render({
            'widgetType': 'block',
            'url': path('sweettooth_loyalty_widget_points_balance', {contactId: entity.id}),
            'title': 'Points Balance',
            'alias': 'contact_points_balance'
        }) }}
    {% endset %}
    {{ UI.scrollSubblock(null, [pointsBalanceWidget]) }}
{% endif %}